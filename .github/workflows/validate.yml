name: Validate

on:
  pull_request:
  push:
    branches: [main, master]

concurrency:
  group: validate-${{ github.ref }}
  cancel-in-progress: true

jobs:
  check:
    uses: juninmd/base-actions/.github/workflows/reusable-validate.yml@2d78bb7febae720e8ab8075ec6ae6e9785c0f6d2
